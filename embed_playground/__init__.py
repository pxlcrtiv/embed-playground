"""embed-playground — compare lexical and dense semantic search, on-device.

Deterministic TF-IDF retrieval out of the box (stdlib-only), an optional
sentence-transformers dense tier (keyless, CPU), an RRF hybrid, and a
bundled retrieval benchmark over an original DeFi/Web3 corpus.
"""

__version__ = "0.1.0"